[package]
name = "tors-fuzz"
version = "0.0.0"
publish = false
edition = "2024"

[package.metadata]
cargo-fuzz = true

[dependencies]
arbitrary = { version = "1", features = ["derive"] }
libfuzzer-sys = "0.4"
# The normalize target's independent digest oracle: the same crates (and
# versions) the main crate uses for finalize's hash, so the fuzz check
# hashes exactly the way the code under test does.
sha2 = "0.10"
const-hex = "1.19"
# The chunk_hierarchical target's independent cluster oracle: the same
# crate (and version) the main crate's grapheme machinery runs on, so the
# fuzz check answers "is this a cluster boundary" exactly the way
# production's tables do, without trusting the code under test to
# re-answer its own question.
unicode-segmentation = "1.13.3"

[dependencies.tors]
# The crates.io package is `tors-core` (the plain `tors` name is an unrelated,
# dormant crate — see the main Cargo.toml's own comment); `package` renames the
# dependency back to `tors` locally so every fuzz target's `use tors::...` stays
# unchanged.
package = "tors-core"
path = ".."
# The library target only: pulling in `extension-module` would try to link
# libpython, which a bare fuzz binary neither has nor needs (the same reason
# `cargo test`/`cargo build` on this crate run --no-default-features).
default-features = false

[[bin]]
name = "decode_utf8"
path = "fuzz_targets/decode_utf8.rs"
test = false
doc = false
bench = false

[[bin]]
name = "decode_utf16"
path = "fuzz_targets/decode_utf16.rs"
test = false
doc = false
bench = false

[[bin]]
name = "b64_decode"
path = "fuzz_targets/b64_decode.rs"
test = false
doc = false
bench = false

[[bin]]
name = "html_unescape"
path = "fuzz_targets/html_unescape.rs"
test = false
doc = false
bench = false

[[bin]]
name = "fence"
path = "fuzz_targets/fence.rs"
test = false
doc = false
bench = false

[[bin]]
name = "chunk_hierarchical"
path = "fuzz_targets/chunk_hierarchical.rs"
test = false
doc = false
bench = false

[[bin]]
name = "normalize"
path = "fuzz_targets/normalize.rs"
test = false
doc = false
bench = false

[[bin]]
name = "search"
path = "fuzz_targets/search.rs"
test = false
doc = false
bench = false

[[bin]]
name = "segmentation"
path = "fuzz_targets/segmentation.rs"
test = false
doc = false
bench = false

[[bin]]
name = "diff"
path = "fuzz_targets/diff.rs"
test = false
doc = false
bench = false

[[bin]]
name = "grounded"
path = "fuzz_targets/grounded.rs"
test = false
doc = false
bench = false

[[bin]]
name = "phonetic"
path = "fuzz_targets/phonetic.rs"
test = false
doc = false
bench = false

[[bin]]
name = "bm25"
path = "fuzz_targets/bm25.rs"
test = false
doc = false
bench = false

[[bin]]
name = "tfidf"
path = "fuzz_targets/tfidf.rs"
test = false
doc = false
bench = false

[[bin]]
name = "truncate_ellipsis"
path = "fuzz_targets/truncate_ellipsis.rs"
test = false
doc = false
bench = false

[[bin]]
name = "controls"
path = "fuzz_targets/controls.rs"
test = false
doc = false
bench = false

[[bin]]
name = "json_repair"
path = "fuzz_targets/json_repair.rs"
test = false
doc = false
bench = false
